name: Tests

on:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: Tests
    uses: frappe/frappe/.github/workflows/_base-server-tests.yml@develop
    with:
      enable-postgres: true
      parallel-runs: 2
    # Remove undefined inputs: disable-socketio and disable-redis-socketio
